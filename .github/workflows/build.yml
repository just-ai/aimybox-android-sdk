name: Aimybox android sdk CI

on:
  push:
    branches:
      - master
      - dev
      - release/*
      - INT-184-github-actions

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'

      - name: Build with gradle
        run: |
          chmod +x gradlew
          ./gradlew build

      - name: show tasks
        run: |
          ./gradlew tasks --all

      - name: Publish with gradle
        env:
          SECRING_MEM_GPG: ${{ secrets.SECRING_MEM_GPG }}
          LOCAL_PROPERTIES: ${{ secrets.LOCAL_PROPERTIES }}
        run: |
          echo "${{ env.SECRING_MEM_GPG }}" > ./secring-mem.gpg
          echo "${{ env.LOCAL_PROPERTIES }}" > ./local.properties
          echo signing.secretKeyRingFile=${{ github.workspace }}/secring-mem.gpg >> ./local.properties
          ./gradlew publishDialogflow-apiPublicationToMavenCentralRepository

#      - name: Validate Gradle wrapper
#        uses: gradle/wrapper-validation-action@e6e38bacfdf1a337459f332974bb2327a31aaf4b
#
#      - name: Publish packages
#        uses: gradle/gradle-build-action@937999e9cc2425eddc7fd62d1053baf041147db7
#        with:
#          arguments: publish


#      - name: Publish with gradle
#        run: |
#          ./gradlew publish